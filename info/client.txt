Lobby:

    onconnect:
        {
            status : 'lobby',
            roomid : roomID,
            playerName : name (name that joined)
        }

    leave:
        {
            status : 'lobby',
            roomid : roomID,
            playerName : name(name that left)
        }

    